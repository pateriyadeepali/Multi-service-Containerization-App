name: quiz_frontend
description: "A Flutter Web Quiz App."

publish_to: 'none' # private package

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # HTTP requests for API
  http: ^1.2.0

  # Cupertino icons for iOS style icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
